## Summary

What changed and why.

## Validation

- [ ] `make verify` passes (all five stages)
- [ ] For question changes: `python3 run_benchmark.py --dry-run` shows no Taboo violations

## Test Details

- **Commands run:**
- **API-backed tests executed?** (yes/no — if yes, which models and run IDs)
- **Benchmark metrics affected?** (yes/no — if yes, before/after)

## Notes

Any additional context for reviewers.
